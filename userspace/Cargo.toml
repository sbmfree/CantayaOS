[workspace]
members = [
    "libcantaya",
    "init",
    "hello",
    "shell_hello",
    "echo",
    "cat",
    "draw",
    "http_get",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "z"
