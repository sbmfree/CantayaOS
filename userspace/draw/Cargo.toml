[package]
name = "draw"
version = "0.1.0"
edition = "2021"
description = "Pixel buffer drawing demo — renders gradient pattern in a GUI window"

[dependencies]
libcantaya = { path = "../libcantaya" }

[[bin]]
name = "draw"
path = "src/main.rs"
